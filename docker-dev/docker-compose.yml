version: '3.1'

volumes:
  pg_selectel:

services:
  pg_db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_PASSWORD=123
      - POSTGRES_USER=123
      - POSTGRES_DB=123
    volumes:
      - ./:/app
    ports:
      - ${POSTGRES_PORT:-5432}:1234